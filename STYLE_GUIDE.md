# 工作室物件风格 · 教材视觉规范 v2

当前基线：第一讲采用 Figma 原生矢量物件、独立中文手写文字和本地场景组合。旧的多工具混合样图归档，不再作为后续课程默认模板。

## 先画什么，再考虑怎么画

每张图只解决一个学习问题。默认先检索 [素材库](assets/library/index.html)，而不是从矩形、箭头和英文名开始。能用短表格讲清的精确映射，不必挤进插图。

图 1–6 分别承担：直觉对比、职责区分、插件化动机、配置顺序、源码导航、运行故事。不是每一讲都必须画六张图。

## 独立物件与统一样式

机器人代表整体助手，大脑代表推理生成；空工具箱代表运行支撑；扳手做动作，手册教方法，日志记过程，资料夹提供材料，盾牌代表约束；插拔模块和工作台表达组织关系。

一个 SVG 只装物件，不把标签或箭头焊进去。catalog.json 记录稳定 ID、中文名、英文概念、关键词、比喻边界和 Figma 节点。scene.json 只引用物件 ID 和摆放方式。

| 样式 | 规范 |
|---|---|
| 纸张与墨色 | 奶油纸 #FFF9EE；暖灰线条 #50483F |
| 物件色 | 薄荷绿、珊瑚粉、灰蓝、淡紫、柔黄 |
| 轮廓 | 圆角、少量不规则、适度留白；不加霓虹或 3D 阴影 |
| 字体 | 本地 Workshop Hand：LXGW WenKai TC 的重命名子集 |
| 代码 | 等宽字体，准确保留源码符号 |
| 文字 | 中文优先；英文首次定义后再用于源码定位 |
| 编辑性 | 原生 SVG 路径 + 独立文字层 + 场景配方，不用“SVG 包了一张图”冒充矢量 |

字体附 [OFL 授权](assets/fonts/OFL.txt) 与来源哈希。不依赖用户电脑是否安装相同字体，不调用远程字体服务。

## 组合规则

1. 一个概念在全书里保持同一形象；对新情境只改变位置、大小或明确登记的变体。
2. 每张图优先不超过七个主要物件，或四个易区分的故事场景；装饰路径不算概念。
3. 关系必须有依据：组合、配置应用、阅读顺序、运行时序不是同一种箭头。
4. 需要时才走工具分支；允许拒绝、失败与直接回答，不画成一条必然成功的流水线。
5. 隐去标签后仍应认得物件。图注写结论和比喻失效点，长解释留在正文。
6. 别把 Harness 画成只有 Tool；别把 Cordis 画成 OS；别把手册画成执行器。

## 字号与窄屏

标准主图为 1200×740。主要标签约 24–31 px，标题约 39 px；按教材实际显示宽度再检查。宽图在手机保留约 1080 px 的可读宽度，使用带提示的局部横向滚动，不把整页撑宽。

## 验收

每张 SVG 有 title、desc、role 和有效 aria 标签。检查中文、线条填充、物件比例、文字碰撞、图形与标签是否相挡。桌面 1440×900、窄屏 390×844 均需真实截图。

自动检查、作者视觉复核、用户认可分开记录。第一讲已做作者复核，不把新整讲擅自标记为用户验收通过。
