# DeepSeek Harness 源码教材视觉规范

> 状态：Lesson 01 的 editorial、Excalidraw、draw.io 三种样板已通过首轮渲染后固化。后续课程默认继承；只有经过真实样图和视觉 QA 验证的调整才能写回本文件。

## 1. 教学目标优先于工具

每张图只回答一个问题。先写清问题、源码 Claim ID、教学任务和比喻边界，再决定工具。没有降低理解成本的图应删除，能用两列表格说清的内容不画图。

默认一讲保留 4–7 张主图；Mermaid 通常不超过 1–2 张，只用于时间顺序、状态或真正简单的流程。

## 2. 固定视觉语义

| 概念 | 颜色 | 形状 / 角色 |
|---|---|---|
| Model | `#EB6C36` | 单一橙色核心；推理与生成焦点 |
| Harness | `#2D3142` | 深色虚线边界或组织场；不得画成普通同级框 |
| Cordis | `#4F5D75` | 共享 Context / 插件树边界；不是操作系统 |
| Plugin | `#6C63A8` | 紫色可装配模块或模块架 |
| Tool / 外部能力 | `#2A9D8F` | 绿色动作端口 |
| Skill | `#2A9D8F` + 文档语义 | 操作手册或指令卡；不画成可执行工具 |
| Session | `#3A7CA5` | 蓝色有序记录或日志 |
| Sandbox / 风险边界 | `#C94C4C` | 红色虚线策略边界 |
| 页面底色 | `#F5F5F5` | 平静纸张；不使用科技感暗色霓虹 |

颜色不能单独承载含义，必须同时配合标签、形状或线型。

## 3. 字体与文字

- 提交的 HTML/SVG 不依赖远程字体、远程图片或运行时网络。
- 中文优先：`"Noto Sans SC", "Microsoft YaHei", system-ui, sans-serif`。
- 标题可用：`Georgia, "Noto Serif SC", "Songti SC", serif`。
- 技术路径和短标签：`"Cascadia Mono", Consolas, monospace`。
- 正文和节点名不使用等宽字体；图中文字在目标宽度下不小于 14 px。
- 图内只放短标签，完整解释留在 Markdown 正文和图注。

## 4. 三种主图语言

### Editorial HTML/SVG

- 用于职责对比、概念关系、源码阅读路线。
- 默认 `1200 × 700` 左右、4 px 网格、密度约 4/10。
- 最多 9 个节点、12 条连线、2 个强调元素；第一讲的概念图优先控制在 7 个主元素内。
- 所有非同轴连线使用圆角正交路径；箭头标签与线保持 6–10 px 间距。
- HTML 保留可编辑源，SVG 用于 Markdown；两者同名并排存放。

### Excalidraw

- 只用于空间隐喻、第一印象和可记忆的白板解释，不承担正式架构事实。
- 使用 `fillStyle: solid`、最小 40 px 间距、正文不小于 16 px。
- 长跨区箭头默认删除；优先让空间位置表达关系。
- 每个重要比喻必须在图内或紧邻图注写出“比喻边界”。

### draw.io

- 用于可长期维护、后续课程可扩展的正式架构母图。
- 使用原生 `.drawio` XML；节点按真实包含或依赖关系分组。
- 连接线必须标明 `配置/组合`、`provide/inject`、`记录`、`调用` 等真实语义。
- 无 draw.io Desktop CLI 时直接生成 XML，并同时维护同名 SVG；不得宣称运行过 CLI 导出。

## 5. 图文配合模板

每张主图在 Markdown 中使用以下结构：

```markdown
### 先问一个问题

<用 2–4 句制造认知缺口，不提前给完定义。>

![能独立说明内容的替代文本](../assets/lesson-NN/fig-NN-name.svg)

> 图 NN：一句话结论。若为比喻图，再补一句“它在哪里失效”。

**回到源码：** <Claim ID + 固定 commit 链接 + 真实术语。>
```

同一张图可在课件和逐步讲解中复用，不复制一张“意思差不多”的新图。

## 6. 响应式规则

- 桌面验收视口：`1440 × 900`；窄屏：`390 × 844`。
- 宽图在窄屏保持字号并允许横向滚动，必须显示“左右滑动查看完整对比”提示。
- 不通过缩小到不可读字号来强行塞入手机宽度。
- 图必须在 3 秒内找到主题、10 秒内说出关系、30 秒内复述结论。

## 7. 无障碍与交付门禁

- SVG 必须包含 `<title>`、`<desc>`、`role="img"` 和有效的 `aria-labelledby`。
- Markdown 图片必须有描述性 alt 文本和结论型图注。
- 可编辑源与导出同名并排；不得存在外部 `http(s)` 依赖。
- 保留一份本地 QA 截图到忽略目录或 `output/playwright/`，不把 QA 截图当教材资产。
- 最终运行项目 Skill 的 `validate-lesson.ps1`，再用 Playwright 检查完整教材而不是只看单图。

## 8. 已验证的反例

- 不让箭头横穿 Session、Plugin 或 Capability 节点。
- 不把标签放在随后绘制的节点下面；节点会遮掉标签的一部分。
- 不用长箭头把所有元素全部连成网；删线通常比加弯折更清楚。
- 不把 Cordis 画成 OS、服务器或 Sandbox。
- 不把 Tool 与 Skill 画成同一种可执行对象。
- 不把 `Agentic system ≈ Model + Harness` 写成源码 `Agent` 接口定义。
