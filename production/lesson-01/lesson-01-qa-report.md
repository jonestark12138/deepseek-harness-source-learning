# 第一讲 QA 报告 · 物件素材库版

## 结论与边界

六份正式教材、11 个独立物件、六张组合图和整讲 HTML 已完成源码核对、构建测试与作者视觉复核。新整讲仍待用户实际阅读反馈；不把机器检查当成学习效果证明。

源码快照：cd5ef8148158c3a752a658978873241fdf8e2bbc。上游检出目录保持只读且工作区干净。教学“测试排查”未实际执行，正文已明确标注。

## 实际完成的检查

| 检查 | 结果 |
|---|---|
| npm ci / 构建 | 成功；依赖锁定，生成六张 SVG、图库和完整 HTML |
| Node 测试 | 11/11 通过：检索、路径边界、确定性、重复 ID、未知素材、透明填充继承、图形边界、箭头语义、缺字与 XML 转义 |
| Lesson validator | 6 个正式 Markdown、23 个 SVG（含 6 个旧归档图）通过，0 warning |
| Skill quick_validate.py | 通过；使用 UTF-8 模式与临时 PyYAML 依赖 |
| 新课脚手架 | 在忽略目录创建六份正式文档与五份制作记录；重复运行拒绝覆盖 |
| 干净发布副本 | 从暂存区导出 89 个文件，在隔离目录重新构建并运行 11 项测试；全部通过 |
| 发布清单与链接 | 81 个本地 Markdown 链接通过；排除 output、.local、node_modules、浏览器目录和上游源码；未发现凭据模式 |
| 字体 | 使用本地手写子集；正文和图中文字有字形覆盖检查，字体带 OFL 和来源哈希 |
| 素材 | 11 个物件均有真实 Figma 节点、原生 SVG 导出、稳定 ID、关键词、边界与 SHA-256 |
| 源码 | 入口分支、补丁顺序、base 六个 ID、Turn/Step 定义与各核心包职责已核对 |

## 浏览器复验

1440×900 桌面与 390×844 窄屏使用隔离 Chrome。浏览器配置、临时依赖和截图缓存在 D 盘的本仓库忽略目录；不上传浏览器资料。

- 六张 SVG：字体均加载，文字越界 0、文字互相重叠 0。
- 六张手机图均检查左/右端：容器 328 px，图宽 1080 px，图在局部滚动。
- 整页宽度与图库宽度均为 390 px，无页面级横向溢出。
- 六份文档导航定位正常，课件起点约在视口 y=82 px；答案折叠可打开。
- 课件与讲解共 12 处图片引用全部加载。
- 最后一轮页面 JavaScript 错误 0，失败资源响应 0。首轮直接打开独立 SVG 曾出现浏览器 favicon.ico 的无关 404；并非教材图片缺失。

已保留精简证据：[桌面课件](qa/lesson-desktop.png)、[手机正文](qa/lesson-mobile.png)、[素材库](qa/library-desktop.png)、[手机宽图](qa/figure-mobile.png)。完整中间截图只保留在本地 output/qa/。

## 真实修复记录

1. 组合器剥离 SVG 外层时丢了 fill=none，开放轮廓被浏览器默认填黑。已保留透明填充继承，并添加回归测试。
2. 初版工作台比例导致桌腿挡住标签。已回到 Figma 制作宽台面版本并重新导出。
   素材文件通过 .gitattributes 固定 LF，避免 Windows 检出转换换行后导致内容哈希变化。
3. HTML 的 base URL 曾影响片内导航；现在链接明确指向预览文档。
4. 平滑滚动让即时截图停在旧位置；已采用即时导航并断言目标位置。

## 目录整理

- 正式第一讲目录仅保留 00–05 六份材料。
- 制作记录归入 production/lesson-01/。
- 13 个旧版图源文件移动到 archive/lesson-01-v1/assets/，可恢复；没有删除旧素材内容。
- 原始需求移动到 plan/archive/，并注明旧工具配额要求已被后续需求更新。
- 早期试画和旧 QA/HTML 移到本地 .local/experiments/lesson-01-2026-09-04/；不再进入当前发布目录。
- 删除了空的 diagrams 目录，无内容损失。旧版完整教材仍可从 Git 的 3121f2e 恢复。

## 工具使用说明

Figma 完成原生矢量创建、分拆、比例调整、截图与导出；没有调用图像生成模型。局部曾受 Starter 额度限制，用户开通会员后真实导出已成功。最终场景由本地 SVG 组合器生成，不冒充已经逐张同步到 Figma 的整页设计。

Excalidraw、draw.io、Mermaid 的旧样图被保留归档，本轮不为了工具数量而重复转绘。
